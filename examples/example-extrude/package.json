{
  "name": "@tstackgl/examples-extrude-demo",
  "version": "0.0.0",
  "description": "extrude demo",
  "author": "nkint <nootropic.kint@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .cache build out",
    "build": "yarn clean && parcel build src/index.html -d out --no-cache --detailed-report --public-url ./",
    "start": "parcel src/index.html --open"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3",
    "terser": "^3.11.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "@tstackgl/geometry": "^0.0.0",
    "@tstackgl/regl-draw": "^0.0.0",
    "@tstackgl/types": "^0.0.0",
    "regl": "^1.3.11",
    "regl-camera": "^2.1.1"
  },
  "browserslist": [
    "last 3 Chrome versions"
  ],
  "browser": {
    "process": false
  }
}
